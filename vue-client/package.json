{
  "name": "moonlight-vue-client",
  "version": "1.0.0",
  "description": "Vue 3 + Moonlight Web 云游戏客户端",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && npm run copy-release-static",
    "copy-release-static": "node --input-type=module -e \"import fs from 'node:fs'; import path from 'node:path'; const src = path.resolve(process.cwd(), 'dist'); const dst = path.resolve(process.cwd(), '..', 'target', 'release', 'static'); fs.rmSync(dst, { recursive: true, force: true }); fs.mkdirSync(dst, { recursive: true }); fs.cpSync(src, dst, { recursive: true });\"",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0"
  }
}
